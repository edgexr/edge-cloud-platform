// Code generated by protoc-gen-gogo. DO NOT EDIT.
// source: svcnode.proto

package notify

import (
	"context"
	fmt "fmt"
	edgeproto "github.com/edgexr/edge-cloud-platform/api/edgeproto"
	"github.com/edgexr/edge-cloud-platform/pkg/log"
	_ "github.com/edgexr/edge-cloud-platform/tools/protogen"
	_ "github.com/gogo/googleapis/google/api"
	_ "github.com/gogo/protobuf/gogoproto"
	proto "github.com/gogo/protobuf/proto"
	"github.com/gogo/protobuf/types"
	opentracing "github.com/opentracing/opentracing-go"
	math "math"
	"sync"
)

// Reference imports to suppress errors if they are not otherwise used.
var _ = proto.Marshal
var _ = fmt.Errorf
var _ = math.Inf

// Auto-generated code: DO NOT EDIT

type SendSvcNodeHandler interface {
	GetAllLocked(ctx context.Context, cb func(key *edgeproto.SvcNode, modRev int64))
	GetWithRev(key *edgeproto.SvcNodeKey, buf *edgeproto.SvcNode, modRev *int64) bool
}

type RecvSvcNodeHandler interface {
	Update(ctx context.Context, in *edgeproto.SvcNode, rev int64)
	Delete(ctx context.Context, in *edgeproto.SvcNode, rev int64)
	Prune(ctx context.Context, keys map[edgeproto.SvcNodeKey]struct{})
	Flush(ctx context.Context, notifyId int64)
}

type SvcNodeCacheHandler interface {
	SendSvcNodeHandler
	RecvSvcNodeHandler
	AddNotifyCb(fn func(ctx context.Context, obj *edgeproto.SvcNode, modRev int64))
}

type SvcNodeSend struct {
	Name        string
	MessageName string
	handler     SendSvcNodeHandler
	Keys        map[edgeproto.SvcNodeKey]SvcNodeSendContext
	keysToSend  map[edgeproto.SvcNodeKey]SvcNodeSendContext
	notifyId    int64
	Mux         sync.Mutex
	buf         edgeproto.SvcNode
	SendCount   uint64
	sendrecv    *SendRecv
}

type SvcNodeSendContext struct {
	ctx         context.Context
	modRev      int64
	forceDelete bool
}

func NewSvcNodeSend(handler SendSvcNodeHandler) *SvcNodeSend {
	send := &SvcNodeSend{}
	send.Name = "SvcNode"
	send.MessageName = proto.MessageName((*edgeproto.SvcNode)(nil))
	send.handler = handler
	send.Keys = make(map[edgeproto.SvcNodeKey]SvcNodeSendContext)
	return send
}

func (s *SvcNodeSend) SetSendRecv(sendrecv *SendRecv) {
	s.sendrecv = sendrecv
}

func (s *SvcNodeSend) GetMessageName() string {
	return s.MessageName
}

func (s *SvcNodeSend) GetName() string {
	return s.Name
}

func (s *SvcNodeSend) GetSendCount() uint64 {
	return s.SendCount
}

func (s *SvcNodeSend) GetNotifyId() int64 {
	return s.notifyId
}
func (s *SvcNodeSend) UpdateAll(ctx context.Context) {
	if !s.sendrecv.isRemoteWanted(s.MessageName) {
		return
	}
	s.Mux.Lock()
	s.handler.GetAllLocked(ctx, func(obj *edgeproto.SvcNode, modRev int64) {
		s.Keys[*obj.GetKey()] = SvcNodeSendContext{
			ctx:    ctx,
			modRev: modRev,
		}
	})
	s.Mux.Unlock()
}

func (s *SvcNodeSend) Update(ctx context.Context, obj *edgeproto.SvcNode, modRev int64) {
	if !s.sendrecv.isRemoteWanted(s.MessageName) {
		return
	}
	forceDelete := false
	s.updateInternal(ctx, obj.GetKey(), modRev, forceDelete)
}

func (s *SvcNodeSend) ForceDelete(ctx context.Context, key *edgeproto.SvcNodeKey, modRev int64) {
	forceDelete := true
	s.updateInternal(ctx, key, modRev, forceDelete)
}

func (s *SvcNodeSend) updateInternal(ctx context.Context, key *edgeproto.SvcNodeKey, modRev int64, forceDelete bool) {
	s.Mux.Lock()
	log.SpanLog(ctx, log.DebugLevelNotify, "updateInternal SvcNode", "key", key, "modRev", modRev)
	s.Keys[*key] = SvcNodeSendContext{
		ctx:         ctx,
		modRev:      modRev,
		forceDelete: forceDelete,
	}
	s.Mux.Unlock()
	s.sendrecv.wakeup()
}

func (s *SvcNodeSend) SendForCloudlet(ctx context.Context, action edgeproto.NoticeAction, cloudlet *edgeproto.Cloudlet) {
}

func (s *SvcNodeSend) Send(stream StreamNotify, notice *edgeproto.Notice, peer string) error {
	s.Mux.Lock()
	keys := s.keysToSend
	s.keysToSend = nil
	s.Mux.Unlock()
	for key, sendContext := range keys {
		ctx := sendContext.ctx
		found := s.handler.GetWithRev(&key, &s.buf, &notice.ModRev)
		if found && !sendContext.forceDelete {
			notice.Action = edgeproto.NoticeAction_UPDATE
		} else {
			notice.Action = edgeproto.NoticeAction_DELETE
			notice.ModRev = sendContext.modRev
			s.buf.Reset()
			s.buf.SetKey(&key)
		}
		any, err := types.MarshalAny(&s.buf)
		if err != nil {
			s.sendrecv.stats.MarshalErrors++
			err = nil
			continue
		}
		notice.Any = *any
		notice.Span = log.SpanToString(ctx)
		log.SpanLog(ctx, log.DebugLevelNotify,
			fmt.Sprintf("%s send SvcNode", s.sendrecv.cliserv),
			"peerAddr", peer,
			"peer", s.sendrecv.peer,
			"local", s.sendrecv.name,
			"action", notice.Action,
			"key", key,
			"modRev", notice.ModRev)
		err = stream.Send(notice)
		if err != nil {
			s.sendrecv.stats.SendErrors++
			return err
		}
		s.sendrecv.stats.Send++
		// object specific counter
		s.SendCount++
	}
	return nil
}

func (s *SvcNodeSend) PrepData() bool {
	s.Mux.Lock()
	defer s.Mux.Unlock()
	if len(s.Keys) > 0 {
		s.keysToSend = s.Keys
		s.Keys = make(map[edgeproto.SvcNodeKey]SvcNodeSendContext)
		return true
	}
	return false
}

// Server accepts multiple clients so needs to track multiple
// peers to send to.
type SvcNodeSendMany struct {
	handler SendSvcNodeHandler
	Mux     sync.Mutex
	sends   map[string]*SvcNodeSend
}

func NewSvcNodeSendMany(handler SendSvcNodeHandler) *SvcNodeSendMany {
	s := &SvcNodeSendMany{}
	s.handler = handler
	s.sends = make(map[string]*SvcNodeSend)
	return s
}

func (s *SvcNodeSendMany) NewSend(peerAddr string, notifyId int64) NotifySend {
	send := NewSvcNodeSend(s.handler)
	send.notifyId = notifyId
	s.Mux.Lock()
	s.sends[peerAddr] = send
	s.Mux.Unlock()
	return send
}

func (s *SvcNodeSendMany) DoneSend(peerAddr string, send NotifySend) {
	asend, ok := send.(*SvcNodeSend)
	if !ok {
		return
	}
	// another connection may come from the same client so remove
	// only if it matches
	s.Mux.Lock()
	if remove, _ := s.sends[peerAddr]; remove == asend {
		delete(s.sends, peerAddr)
	}
	s.Mux.Unlock()
}
func (s *SvcNodeSendMany) Update(ctx context.Context, obj *edgeproto.SvcNode, modRev int64) {
	s.Mux.Lock()
	defer s.Mux.Unlock()
	for _, send := range s.sends {
		send.Update(ctx, obj, modRev)
	}
}

func (s *SvcNodeSendMany) GetTypeString() string {
	return "SvcNode"
}

type SvcNodeRecv struct {
	Name        string
	MessageName string
	handler     RecvSvcNodeHandler
	sendAllKeys map[edgeproto.SvcNodeKey]struct{}
	Mux         sync.Mutex
	buf         edgeproto.SvcNode
	RecvCount   uint64
	sendrecv    *SendRecv
}

func NewSvcNodeRecv(handler RecvSvcNodeHandler) *SvcNodeRecv {
	recv := &SvcNodeRecv{}
	recv.Name = "SvcNode"
	recv.MessageName = proto.MessageName((*edgeproto.SvcNode)(nil))
	recv.handler = handler
	return recv
}

func (s *SvcNodeRecv) SetSendRecv(sendrecv *SendRecv) {
	s.sendrecv = sendrecv
}

func (s *SvcNodeRecv) GetMessageName() string {
	return s.MessageName
}

func (s *SvcNodeRecv) GetName() string {
	return s.Name
}

func (s *SvcNodeRecv) GetRecvCount() uint64 {
	return s.RecvCount
}

func (s *SvcNodeRecv) Recv(ctx context.Context, notice *edgeproto.Notice, notifyId int64, peerAddr string) {
	span := opentracing.SpanFromContext(ctx)
	if span != nil {
		span.SetTag("objtype", "SvcNode")
	}

	buf := &edgeproto.SvcNode{}
	err := types.UnmarshalAny(&notice.Any, buf)
	if err != nil {
		s.sendrecv.stats.UnmarshalErrors++
		log.SpanLog(ctx, log.DebugLevelNotify, "Unmarshal Error", "err", err)
		return
	}
	buf.NotifyId = notifyId
	if span != nil {
		log.SetTags(span, buf.GetKey().GetTags())
	}
	log.SpanLog(ctx, log.DebugLevelNotify,
		fmt.Sprintf("%s recv SvcNode", s.sendrecv.cliserv),
		"peerAddr", peerAddr,
		"peer", s.sendrecv.peer,
		"local", s.sendrecv.name,
		"action", notice.Action,
		"key", buf.GetKeyVal(),
		"modRev", notice.ModRev)
	if notice.Action == edgeproto.NoticeAction_UPDATE {
		s.handler.Update(ctx, buf, notice.ModRev)
		s.Mux.Lock()
		if s.sendAllKeys != nil {
			s.sendAllKeys[buf.GetKeyVal()] = struct{}{}
		}
		s.Mux.Unlock()
	} else if notice.Action == edgeproto.NoticeAction_DELETE {
		s.handler.Delete(ctx, buf, notice.ModRev)
	}
	s.sendrecv.stats.Recv++
	// object specific counter
	s.RecvCount++
}

func (s *SvcNodeRecv) RecvAllStart() {
	s.Mux.Lock()
	defer s.Mux.Unlock()
	s.sendAllKeys = make(map[edgeproto.SvcNodeKey]struct{})
}

func (s *SvcNodeRecv) RecvAllEnd(ctx context.Context, cleanup Cleanup) {
	s.Mux.Lock()
	validKeys := s.sendAllKeys
	s.sendAllKeys = nil
	s.Mux.Unlock()
	if cleanup == CleanupPrune {
		s.handler.Prune(ctx, validKeys)
	}
}
func (s *SvcNodeRecv) Flush(ctx context.Context, notifyId int64) {
	s.handler.Flush(ctx, notifyId)
}

type SvcNodeRecvMany struct {
	handler RecvSvcNodeHandler
}

func NewSvcNodeRecvMany(handler RecvSvcNodeHandler) *SvcNodeRecvMany {
	s := &SvcNodeRecvMany{}
	s.handler = handler
	return s
}

func (s *SvcNodeRecvMany) NewRecv() NotifyRecv {
	recv := NewSvcNodeRecv(s.handler)
	return recv
}

func (s *SvcNodeRecvMany) Flush(ctx context.Context, notifyId int64) {
	s.handler.Flush(ctx, notifyId)
}
func (mgr *ServerMgr) RegisterSendSvcNodeCache(cache SvcNodeCacheHandler) {
	send := NewSvcNodeSendMany(cache)
	mgr.RegisterSend(send)
	cache.AddNotifyCb(send.Update)
}

func (mgr *ServerMgr) RegisterRecvSvcNodeCache(cache SvcNodeCacheHandler) {
	recv := NewSvcNodeRecvMany(cache)
	mgr.RegisterRecv(recv)
}

func (s *Client) RegisterSendSvcNodeCache(cache SvcNodeCacheHandler) {
	send := NewSvcNodeSend(cache)
	s.RegisterSend(send)
	cache.AddNotifyCb(send.Update)
}

func (s *Client) RegisterRecvSvcNodeCache(cache SvcNodeCacheHandler) {
	recv := NewSvcNodeRecv(cache)
	s.RegisterRecv(recv)
}
