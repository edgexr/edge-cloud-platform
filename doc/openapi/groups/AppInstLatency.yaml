# Auto-generated doc: DO NOT EDIT
openapi: 3.0.3
info:
  description: Master Controller APIs for AppInstLatencys
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html
  title: AppInstLatency APIs
  version: "2.0"
servers:
- description: localhost server
  url: http://localhost:9900
paths:
  /api/v1/auth/ctrl/RequestAppInstLatency:
    post:
      description: Request Latency measurements for clients connected to AppInst
      operationId: RequestAppInstLatency
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrmapiRegionAppInstLatency'
            x-required-fields:
            - region
            - appinstlatency.key.name
            - appinstlatency.key.organization
            - appinstlatency.key.cloudletkey.organization
            - appinstlatency.key.cloudletkey.name
            - appinstlatency.key.cloudletkey.federatedorganization
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EdgeprotoResult'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: Bad Request
        "403":
          description: Forbidden
      security:
      - bearerAuth: []
      summary: RequestAppInstLatency
      tags:
      - AppInstLatency
components:
  schemas:
    EdgeprotoAppInstKey:
      properties:
        cloudlet_key:
          $ref: '#/components/schemas/EdgeprotoCloudletKey'
        name:
          description: App Instance name
          type: string
        organization:
          description: App Instance organization
          type: string
      type: object
    EdgeprotoAppInstLatency:
      properties:
        key:
          $ref: '#/components/schemas/EdgeprotoAppInstKey'
      type: object
    EdgeprotoCloudletKey:
      properties:
        federated_organization:
          description: Federated operator organization who shared this cloudlet
          type: string
        name:
          description: Name of the cloudlet
          type: string
        organization:
          description: Organization of the cloudlet site
          type: string
      type: object
    EdgeprotoResult:
      properties:
        code:
          description: Error code, 0 indicates success, non-zero indicates failure
            (not implemented)
          type: integer
        message:
          description: Message, may be success or failure message
          type: string
      type: object
    OrmapiRegionAppInstLatency:
      properties:
        AppInstLatency:
          $ref: '#/components/schemas/EdgeprotoAppInstLatency'
        Region:
          description: Region name
          type: string
      type: object
    OrmapiResult:
      properties:
        code:
          description: Error code
          type: integer
        message:
          description: Informational message
          type: string
      type: object
  securitySchemes:
    bearerAuth:
      bearerFormat: JWT
      description: JWT token generated by the login API to be included in the http
        header
      scheme: bearer
      type: http
