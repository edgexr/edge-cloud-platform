# Auto-generated doc: DO NOT EDIT
openapi: 3.0.3
info:
  description: Master Controller APIs for FederationGuests
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html
  title: FederationGuest APIs
  version: "2.0"
servers:
- description: localhost server
  url: http://localhost:9900
paths:
  /api/v1/auth/federation/consumer/create:
    post:
      description: Create Federation Guest
      operationId: CreateFederationGuest
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrmapiFederationConsumer'
            x-required-fields:
            - name
            - operatorid
            - partneraddr
            - providerclientid
            - providerclientkey
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: Bad Request
        "403":
          description: Forbidden
      security:
      - bearerAuth: []
      summary: CreateFederationGuest
      tags:
      - FederationGuest
  /api/v1/auth/federation/consumer/delete:
    post:
      description: Delete Federation Guest
      operationId: DeleteFederationGuest
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrmapiFederationConsumer'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: Bad Request
        "403":
          description: Forbidden
      security:
      - bearerAuth: []
      summary: DeleteFederationGuest
      tags:
      - FederationGuest
  /api/v1/auth/federation/consumer/gennotifykey:
    post:
      description: Set Partner Federation API Key
      operationId: GenerateFederationGuestNotifyKey
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrmapiFederationConsumer'
            x-required-fields:
            - name
            - operatorid
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: Bad Request
        "403":
          description: Forbidden
      security:
      - bearerAuth: []
      summary: GenerateFederationGuestNotifyKey
      tags:
      - FederationGuest
  /api/v1/auth/federation/consumer/image/show:
    post:
      description: Show Images onboarded by developers
      operationId: ShowFedGuestImage
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrmapiConsumerImage'
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/OrmapiConsumerImage'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: Bad Request
        "403":
          description: Forbidden
      security:
      - bearerAuth: []
      summary: ShowFedGuestImage
      tags:
      - FederationGuest
  /api/v1/auth/federation/consumer/setapikey:
    post:
      description: Set Partner Federation API Key
      operationId: SetFederationGuestAPIKey
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrmapiFederationConsumer'
            x-required-fields:
            - name
            - operatorid
            - providerclientid
            - providerclientkey
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: Bad Request
        "403":
          description: Forbidden
      security:
      - bearerAuth: []
      summary: SetFederationGuestAPIKey
      tags:
      - FederationGuest
  /api/v1/auth/federation/consumer/show:
    post:
      description: Show Federation Guest
      operationId: ShowFederationGuest
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrmapiFederationConsumer'
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/OrmapiFederationConsumer'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: Bad Request
        "403":
          description: Forbidden
      security:
      - bearerAuth: []
      summary: ShowFederationGuest
      tags:
      - FederationGuest
  /api/v1/auth/federation/consumer/update:
    post:
      description: Update Federation Guest
      operationId: UpdateFederationGuest
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrmapiFederationConsumer'
            x-required-fields:
            - name
            - operatorid
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: Bad Request
        "403":
          description: Forbidden
      security:
      - bearerAuth: []
      summary: UpdateFederationGuest
      tags:
      - FederationGuest
  /api/v1/auth/federation/consumer/zone/deregister:
    post:
      description: DeRegister Partner Federator Zone
      operationId: DeregisterGuestZone
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrmapiFederatedZoneRegRequest'
            x-required-fields:
            - fedguest
            - zones
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: Bad Request
        "403":
          description: Forbidden
      security:
      - bearerAuth: []
      summary: DeregisterGuestZone
      tags:
      - FederationGuest
  /api/v1/auth/federation/consumer/zone/register:
    post:
      description: Register Partner Federator Zone
      operationId: RegisterGuestZone
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrmapiFederatedZoneRegRequest'
            x-required-fields:
            - fedguest
            - region
            - zones
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: Bad Request
        "403":
          description: Forbidden
      security:
      - bearerAuth: []
      summary: RegisterGuestZone
      tags:
      - FederationGuest
  /api/v1/auth/federation/consumer/zone/show:
    post:
      description: Show Federated Partner Zones
      operationId: ShowGuestZone
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrmapiConsumerZone'
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/OrmapiConsumerZone'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrmapiResult'
          description: Bad Request
        "403":
          description: Forbidden
      security:
      - bearerAuth: []
      summary: ShowGuestZone
      tags:
      - FederationGuest
components:
  schemas:
    OrmapiConsumerImage:
      properties:
        Checksum:
          description: MD5 checksum for VM and file-based image types, sha256 digest
            for containers and Helm charts
          type: string
        FederationName:
          description: Federation the image is copied to (FederationGuest)
          type: string
        ID:
          description: ID
          type: string
        Name:
          description: Image name
          type: string
        Organization:
          description: Developer organization that owns the image
          type: string
        SourcePath:
          description: Full path to source image as used in App, i.e. https://vm-registry.domain/org/image.img
          type: string
        Status:
          description: Image status
          readOnly: true
          type: string
        Type:
          description: Image type (DOCKER, HELM, QCOW2, or OVA)
          type: string
        Version:
          description: Image version
          type: string
      type: object
    OrmapiConsumerZone:
      properties:
        ConsumerName:
          description: Name of the Federation Guest
          type: string
        GeoLocation:
          description: GPS co-ordinates associated with the zone (in decimal format)
          type: string
        GeographyDetails:
          description: Geography details
          type: string
        OperatorId:
          description: Guest operator organization
          type: string
        Region:
          description: Region in which zone is instantiated
          type: string
        Status:
          description: Zone status
          readOnly: true
          type: string
        ZoneId:
          description: Zone unique name
          type: string
      type: object
    OrmapiFederatedZoneRegRequest:
      properties:
        FedGuest:
          description: Federation Guest name
          type: string
        Region:
          description: Region to create local cloudlet versions of Host zones
          type: string
        Zones:
          description: Partner federator zones to be registered/deregistered
          items:
            type: string
          nullable: true
          type: array
      type: object
    OrmapiFederationConsumer:
      properties:
        AutoRegisterRegion:
          description: Region used for automatically registered zones
          type: string
        AutoRegisterZones:
          description: Automatically register any zone shared with me
          type: boolean
        CreatedAt:
          description: Time created
          format: date-time
          readOnly: true
          type: string
        FederationContextId:
          description: Federation context id returned by partner
          type: string
        ID:
          description: Unique ID
          minimum: 0
          type: integer
        MyInfo:
          $ref: '#/components/schemas/OrmapiFederator'
        Name:
          description: Unique name of this Federation Guest, will be used as an operator
            org for host's zones
          type: string
        NotifyClientId:
          description: Auth ClientId for notify callbacks to this Guest OP
          type: string
        OperatorId:
          description: Operator Organization that establishes the federation with
            a Host OP
          type: string
        PartnerAddr:
          description: Partner Address
          type: string
        PartnerInfo:
          $ref: '#/components/schemas/OrmapiFederator'
        PartnerTokenUrl:
          description: Partner token URL
          type: string
        ProviderClientId:
          description: Auth ClientId for connecting to the Host OP
          type: string
        ProviderClientKey:
          description: Auth ClientKey for connection to the Host OP (stored in secret
            storage)
          type: string
        Public:
          description: Public means any developer will be able to use the cloudlets,
            otherwise (TODO) allowed developers will need to be added explicitly
          type: boolean
        Status:
          description: Status
          readOnly: true
          type: string
        UpdatedAt:
          description: Time updated
          format: date-time
          readOnly: true
          type: string
      type: object
    OrmapiFederator:
      properties:
        CountryCode:
          description: ISO 3166-1 Alpha-2 code for the country where operator platform
            is located
          type: string
        DiscoveryEndPoint:
          description: IP and Port of discovery service URL of operator platform
          type: string
        FederationId:
          description: Globally unique string used to indentify a federation operator
          type: string
        FixedNetworkIds:
          $ref: '#/components/schemas/PqStringArray'
        InitialDate:
          description: Initial create time to denote time zone
          format: date-time
          type: string
        MCC:
          description: Mobile country code of operator sending the request
          type: string
        MNC:
          $ref: '#/components/schemas/PqStringArray'
      type: object
    OrmapiResult:
      properties:
        code:
          description: Error code
          type: integer
        message:
          description: Informational message
          type: string
      type: object
    PqStringArray:
      items:
        type: string
      nullable: true
      type: array
  securitySchemes:
    bearerAuth:
      bearerFormat: JWT
      description: JWT token generated by the login API to be included in the http
        header
      scheme: bearer
      type: http
